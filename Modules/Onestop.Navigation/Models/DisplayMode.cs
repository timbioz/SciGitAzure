namespace Onestop.Navigation.Models {
    public enum DisplayMode {

        /// <summary>
        /// Display the current, published snapshot of the menu
        /// </summary>
        Current,

        /// <summary>
        /// Display the latest snapshot of the menu
        /// </summary>
        Draft
    }
}